const num = document.querySelectorAll("#cal")
const pan = document.querySelector(".pantalla")

num.forEach(element => {
    element.addEventListener("click", event => {
        // pan.style.color = "red"
    })
});